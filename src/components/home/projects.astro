---
import projects from "../../collections/projects.json";
import Button from "../button.astro";
import Project from "../project.astro";

const {pathname} = Astro.url
const splitProjects = pathname.includes('projects') ? projects : [...projects].splice(0, 3); 
---

<section class="max-w-4xl mx-auto px-7 lg:px-0">
  <h2
    class="text-2xl font-bold leading-10 tracking-tight text-neutral-900 dark:text-neutral-100"
  >
    Things I've Built
  </h2>
  <p class="mb-6 text-base text-neutral-600 dark:text-neutral-400">
    A mix of open-source tools, side projects, and things I built just to solve my own problems.
  </p>
  <div
    class="grid items-stretch w-full sm:grid-cols-2 md:grid-cols-3 gap-7 mt-7"
  >
    {
      splitProjects.map((project) => {
        return (
          <Project
            name={project.name}
            description={project.description}
            image={project.image}
            url={project.url}
            tags={project.tags}
          />
        )
      })
    }
  </div>

  <div class="flex items-center justify-center w-full py-5">
    <Button text="View All My Projects" link="/projects" />
  </div>
</section>
